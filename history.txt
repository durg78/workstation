wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
sudo sh -c 'echo "deb http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list'
sudo apt-get update
sudo apt-get install google-chrome-stable

sudo apt-get install evolution-ews

wget https://sourceforge.net/projects/pacmanager/files/latest/download
sudo dpkg -i pac-*.deb
sudo apt-get -f install

wget https://atom.io/download/deb
sudo dpkg -i atom.deb
sudo apt-get -f install

apt-get install sudo
sudo apt-get install vim

***sudo vim /etc/ssh/ssh_config
ssh-keygen -A
sudo apt-get install ifconfig
sudo apt-get update
sudo apt-get install chrome-gnome-shell

sudo apt-get install realmd adcli sssd
sudo apt-get install samba
sudo apt-get install ntp
***sudo vim /etc/ntp.conf
sudo systemctl restart ntp
sudo mkdir -p /var/lib/samba/private
sudo realm discover domain.com
sudo realm join --user=user domain.com
sudo systemctl enable sssd
sudo systemctl start sssd

***sudo gedit /etc/pam.d/common-session
***sudo gedit /etc/sudoers.d/domain_admins
***sudo gedit /etc/sudoers
sudo systemctl restart sudo.service
